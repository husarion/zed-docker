name: Push builded Docker image

on:
  workflow_dispatch:
    inputs:
      image_name:
        description: "Type Docker image name to push"
        type: string
        default: "zed_image:latest"


jobs:
  build-and-push:
    runs-on: jetson

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker image
        run: |
          docker tag ${{ inputs.image_name }} husarion/zed-jetson:melodic
          docker tag ${{ inputs.image_name }} husarion/zed-jetson:melodic-nightly
          docker tag ${{ inputs.image_name }} husarion/zed-jetson:melodic-3.5.0
          docker tag ${{ inputs.image_name }} husarion/zed-jetson:melodic-3.5.0-20230517
          docker tag ${{ inputs.image_name }} husarion/zed-jetson:melodic-3.5.0-20230517-stable
        
          docker push husarion/zed-jetson:melodic
          docker push husarion/zed-jetson:melodic-nightly
          docker push husarion/zed-jetson:melodic-3.5.0
          docker push husarion/zed-jetson:melodic-3.5.0-20230517
          docker push husarion/zed-jetson:melodic-3.5.0-20230517-stable
